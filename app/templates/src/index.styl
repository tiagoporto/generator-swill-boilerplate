// Helpers
@import "<%= folder.styles %>/settings/_variables"
@import "<%= folder.styles %>/helpers/_helpers"

// Web Fonts
// @import "<%= folder.styles %>/settings/_web-fonts"

// Sprites
// @import "<%= folder.styles %>/helpers/_bitmap-sprite"
// @import "<%= folder.styles %>/helpers/_vector-sprite"

// Vendors
// @import "package_name"; // to import from node_modules

/*! ==================================================================
   Main Styles
   ================================================================== */

/*!
*
* Box model reset
* http://css-tricks.com/inheriting-box-sizing-probably-slightly-better-best-practice/
*
*/
html
  box-sizing: border-box

*, *:before, *:after
  box-sizing: inherit

i[class^="icon-"]
  display: block

@import "<%= folder.styles %>/components/_index"
