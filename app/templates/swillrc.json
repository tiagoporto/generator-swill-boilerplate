{
  "basePaths": {
    "src": "<%= folder.src %>",
    "dest": "<%= folder.dest %>",
    "build": "<%= folder.build %>",
    "images": "<%= folder.images.src %>",
    "fonts": "<%= folder.fonts %>",
    "scripts": "<%= folder.scripts.src %>",
    "styles": "<%= folder.styles.src %>"
  },

  "validateW3C": <%= use.validateHTML %>,

  "lintCSS": <%= use.lint.css %>,

  "lintJS": <%= use.lint.js %>,<% if (use.inlineSVG) { %>

  "inlineSVG": true,<% } %>

  "svgPrefixClass": ".icon-",

  "autoprefixerBrowsers": [
    "ie > 7",
    "ie_mob > 9",
    "Firefox > 24",
    "last 10 Chrome versions",
    "safari > 5",
    "opera > 24",
    "ios > 5",
    "android > 4",
    "bb > 10"
  ],

  "browserSync": {
    "notify": false,
    "logPrefix": "BrowserSync",
    "server": {
      "baseDir": ["<%= folder.dest %>/", "./"]
    },
    "routes": {
        "/bower_components": "bower_components",
        "/node_modules": "node_modules"
    }
  },

  "browserSyncBuild": {
    "notify": false,
    "logPrefix": "BrowserSync",
    "server": {
      "baseDir": ["<%= folder.build %>/"]
    }
  }
}
