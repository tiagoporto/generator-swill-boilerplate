language: node_js

node_js:
  - "stable"<% if (include.readme) { %>

before_install:
  - gem install awesome_bot<% } %>

script:<% if (include.readme) { %>
  - awesome_bot README.md --allow-redirect --white-list travis-ci<% } %>
  - npm run lint && npm test

after_success:
  - npm run coveralls
