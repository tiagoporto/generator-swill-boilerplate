/*
Stylus variables are information about icon's compiled state, stored under its original file name

.icon-home {
  width: $icon_home_width;
}

The large array-like variables contain all information about a single icon
$icon_home = x y offset_x offset_y width height total_width total_height image_path;
*/
$left-arrow_x = 0px;
$left-arrow_y = 0px;
$left-arrow_offset_x = 0px;
$left-arrow_offset_y = 0px;
$left-arrow_width = 32px;
$left-arrow_height = 32px;
$left-arrow_total_width = 32px;
$left-arrow_total_height = 66px;
$left-arrow_image = '../images/sprite.png';
$left-arrow = 0px 0px 0px 0px 32px 32px 32px 66px '../images/sprite.png';
$right-arrow_x = 0px;
$right-arrow_y = 34px;
$right-arrow_offset_x = 0px;
$right-arrow_offset_y = -34px;
$right-arrow_width = 32px;
$right-arrow_height = 32px;
$right-arrow_total_width = 32px;
$right-arrow_total_height = 66px;
$right-arrow_image = '../images/sprite.png';
$right-arrow = 0px 34px 0px -34px 32px 32px 32px 66px '../images/sprite.png';

/*
The provided mixins are intended to be used with the array-like variables

.icon-home {
  spriteWidth($icon_home)
}

.icon-email {
  sprite($icon_email)
}
*/
spriteWidth($sprite) {
  width: $sprite[4];
}

spriteHeight($sprite) {
  height: $sprite[5];
}

spritePosition($sprite) {
  background-position: $sprite[2] $sprite[3];
}

spriteImage($sprite) {
  background-image: url($sprite[8]);
}

sprite($sprite) {
  spriteImage($sprite)
  spritePosition($sprite)
  spriteWidth($sprite)
  spriteHeight($sprite)
}
